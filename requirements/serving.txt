# Shared serving dependencies for both the API and dashboard containers.
#
# Excludes offline-only tools: torch, lightgbm, optuna, mlflow, umap-learn,
# matplotlib, seaborn, tqdm — those belong in requirements/ml.txt or gnn.txt.
# Vina and GNINA are native binaries used only by the docking pipeline.

# Core numeric / data
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
joblib>=1.3.0
scikit-learn>=1.3.0

# XGBoost — needed to deserialise the saved WT-proxy (Model 2) pickle
xgboost>=3.2.0

# Cheminformatics (fingerprints, descriptors, ADMET, covalent detector, AD)
rdkit>=2023.3.1

# Config and environment
pyyaml>=6.0
python-dotenv>=1.0.0
pydantic>=2.4.0
